# Ставится в /etc/systemd/system/caddy.service.d/10-restart.conf
# В штатном юните caddy НЕТ строки Restart -> по умолчанию Restart=no,
# то есть падение Caddy = раздача лежит до ручного подъёма. Чиним.
[Service]
Restart=on-failure
RestartSec=2s
