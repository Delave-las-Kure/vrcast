{
  "_comment": [
    "T252 — из чего состоит серверная часть, которую разворачивает приложение.",
    "",
    "Версия целая и растёт на единицу при любом изменении состава (R-11). Она же",
    "пишется в /etc/vrcast/state.json последним шагом развёртывания и читается при",
    "каждом подключении. Состав замораживается в конце Фазы 7 (T337): после того как",
    "приложение роздано, изменение состава требует обновления у всех.",
    "",
    "MediaMTX здесь нет намеренно (решение владельца 2026-08-27). Приложение не",
    "обратилось к нему ни разу за вехи A-C: зрительские ссылки идут через /videos/",
    "с диска, отрезки лесенки кладутся туда же. В спеке, плане и договорах он не",
    "упомянут. Держать службу, которой никто не пользуется, значит ставить её,",
    "версионировать, чинить и объяснять её отказы в снимке состояния.",
    "",
    "Caddy берётся из официального репозитория, а не закреплённым архивом со слепком",
    "(как FFmpeg в установщике). Причина в FR-126: репозиторий подключён к",
    "автоматическим обновлениям безопасности, а закреплённый архив от них отрезан, и",
    "чинить его пришлось бы обновлением серверной части на каждую заплату. Взамен",
    "проверяется нижняя граница версии после установки: репозиторий может отдать что",
    "угодно, и молча взятое 'что угодно' -- это не основание."
  ],

  "vrcast_server_version": 1,

  "components": [
    {
      "id": "caddy",
      "source": "apt",
      "repository": "https://dl.cloudsmith.io/public/caddy/stable/debian.deb.txt",
      "key": "https://dl.cloudsmith.io/public/caddy/stable/gpg.key",
      "min_version": "2.8.0",
      "why": "раздача: прямой файл и отрезки лесенки с диска, журнал обращений, правила подмены качества"
    },
    {
      "id": "ffmpeg",
      "source": "apt",
      "min_version": "6.0",
      "why": "нарезка отрезков на сервере пересборкой без перекода (T196): сотни мелких файлов рождаются там, где будут отданы"
    },
    {
      "id": "ufw",
      "source": "apt",
      "why": "сетевой фильтр, одинаково для IPv4 и IPv6 (FR-126, FR-136)"
    },
    {
      "id": "fail2ban",
      "source": "apt",
      "why": "перебор паролей забивает слоты неаутентифицированных соединений и рвёт долгие рабочие сессии"
    },
    {
      "id": "unattended-upgrades",
      "source": "apt",
      "why": "автоматические обновления безопасности (FR-126)"
    }
  ],

  "steps": [
    "dns-check",
    "swap",
    "packages",
    "user-dirs",
    "configs",
    "services",
    "ssh-key",
    "ssh-hardening",
    "firewall",
    "ipv6",
    "fail2ban",
    "unattended-upgrades",
    "tuning",
    "verify",
    "state"
  ],

  "files": {
    "/etc/caddy/Caddyfile": "Caddyfile",
    "/etc/caddy/vrcast-limits.conf": "создаётся пустым: импорт несуществующего файла Caddy не принимает",
    "/etc/sysctl.d/99-vrcast-net.conf": "99-vrcast-net.conf",
    "/etc/udev/rules.d/60-vrcast-readahead.rules": "60-vrcast-readahead.rules",
    "/etc/systemd/system/caddy.service.d/10-restart.conf": "caddy-10-restart.conf"
  }
}
